import pandas as pd

def multifunc(a,b) -> int:
    """A function that returns the product of two integers

    Args:
        a (int): the first integer
        b (int): the second integer

    Returns:
        int: the product of integers a and b
    """
    return a * b

print(multifunc(3,7))